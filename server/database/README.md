# Database access

Run: ```node index.js```

In browser:

Get database records:
```localhost:3000/```

Add record:
```localhost:3000/insert/```

Enter a record and submit

Page should redirect back to ```/```